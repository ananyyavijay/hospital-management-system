# name: CD

# on:
#   push:
#     branches:
#       - main              # ONLY fires on push to main
#   workflow_dispatch:      # allow manual trigger for emergency deploys

# jobs:
#   # ── Job 1: Run tests first ─────────────────────────────────────────────────
#   # (identical to ci.yml — CD must re-run tests to be trustworthy)
#   test:
#     name: Run tests before deploy
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Set up Python 3.11
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt

#       - name: Run tests
#         run: pytest tests/ -v --tb=short
#         env:
#           APP_ENV: test

#   # ── Job 2: Deploy to Azure (only if tests passed) ──────────────────────────
#   deploy:
#     name: Deploy to Azure App Service
#     runs-on: ubuntu-latest
#     needs: test             # ← ONLY runs if the test job PASSED
#     environment: production # ← links to GitHub environment (Day 28)

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Deploy to Azure App Service
#         uses: azure/webapps-deploy@v3
#         with:
#           app-name: hms-app-ananya
#           publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
#           package: .

#       - name: Verify deployment
#         run: |
#           echo "Waiting for app to restart..."
#           sleep 30
#           curl -f https://hms-app-ananya.azurewebsites.net/health
#           echo "Deployment verified!"

name: CD
on:
  push:
    branches: 
    - feature/hms-four-new-features
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: pip install -r requirements.txt
      - run: pytest tests/ -v --tb=short
        env: { APP_ENV: test }
  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - uses: azure/webapps-deploy@v3
        with:
          app-name: hms-app-ananya
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
      - name: Verify
        run: |
          sleep 30
          curl -f https://hms-app-ananya.azurewebsites.net/health